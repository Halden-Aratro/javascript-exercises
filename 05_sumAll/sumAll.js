const sumAll = function(min, max) {
    const ARRAY = [];
    let numValue = min;
    let total = min;

    for (i = 0; i < max; i++) {
        ARRAY += [i];
    }
    
    min < 0 || max < 0 ? return;

};

// Do not edit below this line
module.exports = sumAll;
